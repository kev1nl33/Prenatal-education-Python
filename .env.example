# 环境变量配置示例
# 请在Vercel项目设置中配置以下环境变量

# 运行模式配置 (重要: 生产环境必须设置为 prod)
MODE=prod

# 允许的跨域来源
ALLOWED_ORIGIN=https://your-domain.vercel.app

# 鉴权配置 (新增)
# 后端API访问令牌列表，逗号分隔，支持多个令牌
ALLOWED_TOKENS=demo-token-2024,another-token-here
# 前端使用的演示令牌（注入到前端环境变量）
NEXT_PUBLIC_DEMO_TOKEN=demo-token-2024

# 火山方舟API配置
ARK_API_KEY=your_ark_api_key_here
ARK_MODEL=doubao-1.5-pro-32k-250115

# TTS API配置（v1，使用 app_id + access_token）
TTS_ACCESS_TOKEN=your_tts_access_token_here
TTS_APP_ID=your_tts_app_id_here
TTS_RESOURCE_ID=volc.service_type.10029

# TTS 默认配置（可选）
TTS_DEFAULT_VOICE=zh_female_qingxin
TTS_DEFAULT_EMOTION=neutral
TTS_DEFAULT_QUALITY=draft

# 火山引擎SDK签名认证配置（推荐，优先使用）
VOLC_ACCESS_KEY_ID=your_access_key_id_here
VOLC_SECRET_ACCESS_KEY=your_secret_access_key_here

# TTS v1 配置
TTS_SAMPLE_RATE=24000

# 声音复刻配置（可选，如果不设置则使用TTS_API_URL）
VOICE_CLONE_API_URL=https://openspeech.bytedance.com/api/v1/tts